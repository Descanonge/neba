channels:
    - conda-forge
    - nodefaults
dependencies:
    - python = 3.11

    - numpy >= 1.24
    - xarray >= 2023.7
    - dask >= 2023.8

    - traitlets >= 5.13

    - sphinx
    - sphinx-book-theme

    - dask-jobqueue

    - ipython
    - ipykernel

    - ruff
    - mypy >= 1.5
    - pytest >= 7.4
    - hypothesis

    - pip

    - pip:
        - filefinder = 1.2.*
